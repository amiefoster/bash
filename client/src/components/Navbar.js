import { NavLink, Link } from 'react-router-dom'

function Navbar({ user, setUser = () => console.log('') }) {

   

    return (
        <div className="row header-container">
           
        </div>
    )
}

export default Navbar;